# PrepLab

Target environment for **Data & Applications Security**. Runs locally on Docker
Desktop. See `GUIDE.md` for full instructions.

```bash
docker compose up -d
curl http://localhost:8080/health
```

You own this environment, so testing it is authorised. Do not point these
techniques at anything else without written permission.
